name: Build Windows EXE on every commit

on:
  push:
    branches: [main]
    paths-ignore:
      - "README.md"
      - "**/*.md"
      - "spec/**"
      - ".gitignore"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: build-release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller==6.11.1

      # Gate the release on the offscreen suite. Every push to main becomes
      # the "Latest" download, so shipping without running the tests that
      # already exist means users get whatever compiled. QT_QPA_PLATFORM
      # makes the GUI modules run headless on the CI runner.
      - name: Run regression suite
        env:
          QT_QPA_PLATFORM: offscreen
        run: python tests/run_all.py

      - name: Generate Windows version-info file
        shell: bash
        run: |
          mkdir -p build
          short_sha=${GITHUB_SHA:0:7}
          stamp=$(date -u +%Y.%m.%d)
          ver_str="build-${stamp}-${short_sha}"
          cat > build/version.txt <<EOF
          VSVersionInfo(
            ffi=FixedFileInfo(
              filevers=(0, 0, 0, 0),
              prodvers=(0, 0, 0, 0),
              mask=0x3f,
              flags=0x0,
              OS=0x40004,
              fileType=0x1,
              subtype=0x0,
              date=(0, 0)),
            kids=[
              StringFileInfo([
                StringTable(u'040904B0', [
                  StringStruct(u'CompanyName', u'cbolong'),
                  StringStruct(u'FileDescription', u'Arduino應用軟體'),
                  StringStruct(u'FileVersion', u'${ver_str}'),
                  StringStruct(u'InternalName', u'Arduino_Utility'),
                  StringStruct(u'OriginalFilename', u'Arduino_Utility.exe'),
                  StringStruct(u'ProductName', u'Arduino應用軟體'),
                  StringStruct(u'ProductVersion', u'${ver_str}')])]),
              VarFileInfo([VarStruct(u'Translation', [1033, 1200])])])
          EOF

      - name: Build EXE with PyInstaller
        shell: bash
        run: |
          # --onefile chosen for distribution simplicity (single .exe, no
          # surrounding folder). Trade-off: cold-start unpacks runtime to
          # %TEMP% on every launch — larger / slower now that the GUI is
          # PySide6/Qt (~60 MB EXE). PyInstaller's bundled PySide6 hook
          # collects the Qt platform plugin automatically. tkinter is no
          # longer used, so exclude it to trim the bundle.
          pyinstaller --noconfirm --onefile --windowed --clean \
            --name Arduino_Utility \
            --collect-submodules serial \
            --hidden-import serial.tools.list_ports_windows \
            --exclude-module tkinter \
            --noupx \
            --icon assets/icon.ico \
            --add-data "assets/icon.ico;assets" \
            --version-file build/version.txt \
            binFileTransferGui.py

      - name: Compute build tag and commit title
        id: tag
        shell: bash
        env:
          FULL_MSG: ${{ github.event.head_commit.message }}
        run: |
          ts=$(date -u +%Y%m%d-%H%M%S)
          short_sha=${GITHUB_SHA:0:7}
          echo "tag=build-${ts}-${short_sha}" >> "$GITHUB_OUTPUT"

          # Extract just the first line (subject) of the commit message,
          # falling back to the short SHA for events without head_commit
          # (e.g. workflow_dispatch).
          title=$(printf '%s' "$FULL_MSG" | head -n1)
          if [ -z "$title" ]; then
            title="manual build $short_sha"
          fi
          {
            echo "title<<EOF"
            echo "$title"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Create release with EXE
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          name: "Auto build ${{ steps.tag.outputs.tag }}"
          body: ${{ steps.tag.outputs.title }}
          prerelease: false
          make_latest: "true"
          files: dist/Arduino_Utility.exe

      - name: Prune old auto-build assets, keep latest 4
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release list --repo "$GITHUB_REPOSITORY" --limit 200 \
            --json tagName,publishedAt \
            --jq 'map(select(.tagName | startswith("build-"))) |
                  sort_by(.publishedAt) | reverse | .[4:] | .[].tagName' \
          | while read -r tag; do
              [ -z "$tag" ] && continue
              echo "Pruning auto-build assets from $tag"
              # Match the current .exe name plus the legacy names (the old
              # SST39FlashProgrammer.exe and the short-lived onedir .zip) so
              # leftover assets on older releases still get cleaned up.
              gh release view "$tag" --repo "$GITHUB_REPOSITORY" \
                --json assets --jq '.assets[] | select(.name=="Arduino_Utility.exe" or .name=="SST39FlashProgrammer.exe" or .name=="SST39FlashProgrammer.zip") | .name' \
              | xargs -r -I{} gh release delete-asset "$tag" "{}" --repo "$GITHUB_REPOSITORY" --yes
            done
